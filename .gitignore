# Local build plan (not tracked)
BUILD_PLAN.md

# Node / Vite frontend
node_modules
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
*.sw?

# Intermediate icon render (regenerate from icon-source.svg with `tauri icon`)
src-tauri/icon-source.png

# Updater signing key — MUST stay secret. The private key + its password live
# only on this machine and in GitHub Actions secrets. Losing them means future
# releases can't be signed and auto-update breaks. The .pub key is safe and IS
# committed (it's baked into tauri.conf.json).
src-tauri/fidget-updater.key
src-tauri/.updater-key-password

# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
